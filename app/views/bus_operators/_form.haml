= form_with(model: bus_operator, local: true) do |form|
  - if bus_operator.errors.any?
    %div#error_explanation
      %h2= pluralize(bus_operator.errors.count, "error") + " prohibited this bus_operator from being saved:"
      %ul
      - bus_operator.errors.full_messages.each do |message|
        %li= message

  %div.form-group
    = label :description, "Descripción"
    %br
    = text_area(:bus_operator, :description, cols: 80, rows: 10)

  %div.actions
    = submit_tag 'Grabar', class: 'btn btn-primary'
